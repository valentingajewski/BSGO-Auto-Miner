
def scan_asteroid(asteroid_list_temp)
    for asteroid in asteroid_temp_list:
        Click on nearest asteroid
        if distance <= 2000:
            scan
            if scan = scan_choice:
                shoot_asteroid()
                clear asteroid_list_temp 
            else: 
                delete asteroid_coordinates in asteroid_list_temp
                

def mining(status, scan_choice)

    for i = 0, i <= 5, i ++
        detect_asteroid
        Save cursor coordinates + distance to asteroid in asteroid_list_temp list

    delete_doublons(asteroid_list_temp)

    while status = "Mining":
        scan_asteroid(asteroid_list_temp)